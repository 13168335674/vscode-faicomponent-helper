{
	"fa-switch/autoFocus": {
		"description": "get focus when component mounted",
		"optionType": "boolean",
		"defaultValue": "false"
	},
	"fa-switch/checked": {
		"description": "determine whether the Switch is checked",
		"optionType": "boolean",
		"defaultValue": "false"
	},
	"fa-switch/checkedChildren": {
		"description": "content to be shown when the state is checked",
		"optionType": "string|slot",
		"defaultValue": ""
	},
	"fa-switch/defaultChecked": {
		"description": "to set the initial state",
		"optionType": "boolean",
		"defaultValue": "false"
	},
	"fa-switch/disabled": {
		"description": "Disable switch",
		"optionType": "boolean",
		"defaultValue": "false"
	},
	"fa-switch/loading": {
		"description": "loading state of switch",
		"optionType": "boolean",
		"defaultValue": "false"
	},
	"fa-switch/size": {
		"description": "the size of the Switch, options: default small",
		"optionType": "string",
		"defaultValue": "default"
	},
	"fa-switch/unCheckedChildren": {
		"description": "content to be shown when the state is unchecked",
		"optionType": "string|slot",
		"defaultValue": ""
	}
}